server.port=4042

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://notification-db:5432/notification_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
spring.jpa.open-in-view=false

# Swagger/OpenAPI config
springdoc.api-docs.enabled=true
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.paths-to-match=/api/**

# Mail configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=sathurshans04@gmail.com
spring.mail.password=pfil ebmh xhtp vvrp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Actuator configuration
management.endpoints.web.exposure.include=health,prometheus,metrics
management.endpoint.health.show-details=when-authorized
management.health.probes.enabled=true
management.endpoints.web.base-path=/actuator
management.metrics.export.prometheus.enabled=true
management.metrics.export.prometheus.step=10s

# Logging
logging.level.org.springframework=INFO
logging.level.org.shathursan=DEBUG

# Micrometer Tracing configuration (Spring Boot 3.x compatible)
management.tracing.sampling.probability=1.0
management.tracing.enabled=true

# OpenTelemetry exporter configuration
management.otlp.tracing.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://otel-collector:4317}
management.otlp.tracing.protocol=grpc
management.otlp.metrics.endpoint=${OTEL_EXPORTER_OTLP_ENDPOINT:http://otel-collector:4317}

# Service identification
spring.application.name=notification-service