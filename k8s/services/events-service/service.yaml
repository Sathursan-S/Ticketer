apiVersion: v1
kind: Service
metadata:
  name: events-service
  labels:
    app: events-service
spec:
  selector:
    app: events-service
  ports:
    - name: http
      protocol: TCP
      port: 4041
      targetPort: 4041
  type: ClusterIP
